# 统一的 MCP 工具执行动作:所有 executor 都实现此 action,manager 负责路由转发
---
# goal
string tool_name     # 要调用的工具名
string params_json   # 工具参数(JSON 字符串)
string trace_id      # 调用追踪标识(通常为 任务id:节点id)
---
# result
bool success
string result_json   # 结构化执行结果(JSON)
string error         # 失败原因
---
# feedback
string status    # running 等中间状态
string message   # 中间信息(如导航反馈)
